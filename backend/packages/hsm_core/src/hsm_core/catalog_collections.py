"""Firestore collection ids shared by API, worker, and catalog helpers."""

# Single collection for Model documents (document id = Model.id).
MODELS_COLLECTION_ID = "models"
PROJECTS_COLLECTION_ID = "projects"
