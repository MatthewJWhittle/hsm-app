GCP_PROJECT_ID="hsm-dashboard"
ARTIFACT_REGION="us-central1"
ARTIFACT_REPOSITORY="hsm-app"
IMAGE_NAME="backend"

# Optional override. If unset, script uses `git rev-parse --short HEAD`.
# IMAGE_TAG="manual-tag"

# Optional: update terraform tfvars image tags after push.
UPDATE_TFVARS=true
TFVARS_PATH="./infra/terraform/terraform.tfvars"
